function firstChar(text) {
  // your code here
	text = text.trim();
	alert(text);
}

// Do not change the code below
firstChar(text);
// const text = prompt("Enter text:");
// alert(firstChar(text));
